import React from 'react';
import { interpolate, useCurrentFrame } from 'remotion';
export const PhotoKenBurnsMotion:React.FC<{durationFrames:number;intensity:number;children:React.ReactNode}> = ({durationFrames,intensity,children})=>{const f=useCurrentFrame(); const s=interpolate(f,[0,durationFrames],[1.05,1.16+intensity*.08],{extrapolateRight:'clamp'}); const x=interpolate(f,[0,durationFrames],[-18*intensity,18*intensity],{extrapolateRight:'clamp'}); const y=interpolate(f,[0,durationFrames],[10*intensity,-14*intensity],{extrapolateRight:'clamp'}); return <div style={{width:'100%',height:'100%',transform:`scale(${s}) translate(${x}px,${y}px)`}}>{children}</div>};
