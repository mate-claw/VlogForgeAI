import React from 'react'; import { useCurrentFrame } from 'remotion'; export const AudioWaveformSticker:React.FC<{color:string}> = ({color}) => {const f=useCurrentFrame(); return <div style={{position:'absolute',left:80,bottom:120,width:260,height:54,display:'flex',gap:8,alignItems:'center'}}>{Array.from({length:18}).map((_,i)=>{const h=12+Math.abs(Math.sin((f+i*4)/7))*38;return <div key={i} style={{width:7,height:h,borderRadius:8,background:color,boxShadow:`0 0 14px ${color}88`,opacity:.72}}/>})}</div>};
